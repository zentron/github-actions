# action.yml
name: 'GitVersion'
description: 'Calculate the semantic version of the repository based on the git tree'
outputs:
  version:
    description: 'The NuGetVersionV2 version generated by GitVersion'
runs:
  using: 'docker'
  image: 'Dockerfile'